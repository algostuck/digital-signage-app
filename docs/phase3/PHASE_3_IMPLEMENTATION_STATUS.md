# Phase 3 — Implementation Status

Method unchanged from Phases 1–2: vertical slices
(migration → models → services → APIs → tests → frontend → live E2E → docs),
full PostgreSQL regression green after every slice, feature-flag gated,
per-slice security checklist (PHASE_3_SECURITY_MODEL.md §9).

Foundation verified before start: Phase-2 final sign-off **240/240 on
PostgreSQL** (2026-08-29); initiation audit in PHASE_3_INITIATION_REPORT.md.

| # | Slice | SRS modules | Status |
|---|---|---|---|
| 3A-1 | Foundation: ~~tenant feature flags~~ (done pre-Phase-3 by the SaaS-core entitlement engine, `docs/SAAS_CORE.md`), domain event bus (events + subscriptions + signed deliveries), retention keys for new streams | P3-M09 (part) | **Done** (2026-08-30; migration 0022 up/down/up on PG; `app/services/events.py` with 10-type catalogue, emissions wired into devices/monitoring/content/publishing/incidents/subscriptions; delivery beat 60 s; retention keys domain_events 90 d / event_deliveries 30 d; APIs `/events`, `/events/catalogue`, `/subscriptions*`; Event Bus UI in Settings→Integrations; 6 tests in test_event_bus.py incl. signature verify, filter, retry→dead→replay, RBAC, isolation, retention; live E2E verified) |
| 3A-2 | Dynamic data: sources + schemas + guarded fetch + snapshots/cache + transforms + widget bindings + manifest data block; Data Source Manager + widget-designer extension | P3-M02 | **Done** (2026-08-30; migration 0023 up/down/up on PG; `app/services/data_sources.py` + SSRF-guarded `app/integrations/fetch.py` (public-IP check, no redirects, 1 MiB cap); REST/JSON + RSS/Atom parsing; declared-schema validation; safe path/fields/limit transforms; bounded snapshots (last 20) with last-known-good; `dynamic_data` entitlement gate; zone `widget.data_binding` validated at layout publish/template submit; manifest v2 `data` block; 60 s refresh beat; retention key 14 d keeping newest valid; Data Sources UI + designer binding editor; 7 tests in test_data_sources_api.py; live E2E on a real BBC RSS feed incl. SSRF-probe refusal) |
| 3A-3 | Developer platform: versioned OpenAPI publication, sandbox tenant flow, Developer Portal screen | P3-M12, P3-INT-103 | **Done** (2026-08-30; migration 0024 up/down/up on PG; `api_products`/`api_versions` catalogue seeded (Control Plane API v1 current, Player Contract v1 current + v2 preview) with lifecycle + changelog; `GET /developer/openapi`; sandbox = isolated org `<code>-sbx` linked via settings_json, owner access via SaaS-core guest membership (tenant switcher), no subscription → unrestricted; `POST /developer/sandbox` idempotent + `simulate-device` through the real player pipeline; `developer_portal` entitlement + api_keys.manage gate; Developer Portal page; 4 tests in test_developer_api.py; live E2E: sandbox provisioned, SIM device heartbeating) |
| 3B-1 | AI foundation: provider adapter + local deterministic provider, policies/requests/outputs governance, approval adapter, AI Content Studio + Variant Manager | P3-M01 | **Done** (2026-08-30; migration 0025 up/down/up on PG; `app/integrations/ai_providers.py` LocalDeterministicProvider (text templates headline/shorten/cta/tone, dimension-aware creative variants, placeholder-freezing glossary localization for hi/bn/es/fr/de) behind `AI_PROVIDER` config; `app/services/ai/` governance (per-tenant operations/guardrails/approval policies, explainability ledger ai_requests/ai_outputs with provider/model/template-version/confidence) + operations (failure ladder to deterministic fallback, placeholder-integrity check); `ai_output` registered as 2A approval adapter (same inbox as campaigns); `ai_features` entitlement + `ai_credits_month` metering ("AI credit limit reached (N/N)"); `/ai/*` APIs; AI Studio tab on /design with Fact/Recommendation labeling + confidence badges; 7 tests in test_ai_api.py; live E2E) |
| 3B-2 | Decisioning: policies/rules/guardrails, manifest integration with degradation ladder, decision preview + log; Decisioning Rules screen | P3-M03 (part) | **Done** (2026-08-30; migration 0026 up/down/up on PG; `app/services/decisioning.py`: ordered pin/boost/exclude rules over device/location-subtree/tag/time/external-data context (P3-DEC-001 reuses 3A-2 snapshots), deterministic resolution with auditable reasons (P3-DEC-002); guardrails (P3-DEC-004): schedule windows never overridden (rules act only on schedule-eligible candidates), mandatory campaigns never excluded, anti-flapping switch budget counting actual changes; manifest hook with try/except degradation to the 1H scheduler + additive `decision` reasons block; bounded decision_log (retention 30 d) written only on real switches; `/decision-policies`, `/decision-rules/preview`, `/decision-log` APIs; Decisioning tab on /campaigns (policy list, JSON rules editor, dry-run preview, log); 6 tests in test_decisioning_api.py) |
| 3B-3 | Experimentation: experiments/variants/stable assignments, results; Experiment Manager | P3-DEC-003 | **Done** (2026-08-30; migration 0027 up/down/up on PG; `app/services/experiments.py`: A/B arms over 2E campaign variants, allocation ≤100% with implicit control remainder, stable sha256(experiment:device) bucket assignment persisted as evidence, one running experiment per campaign, manifest override with additive `experiment` block + degradation to normal 2E resolution, per-arm results from existing proof-of-play events, `experiments` entitlement gate; `/experiments` APIs; Experiments tab on /campaigns; 3 tests in test_experiments_api.py) |
| 3C-1 | Video walls: walls/members/viewports, sync sessions (markers + tolerance), degraded state + incidents, contract v2 sync block; Wall Manager + Control | P3-M04 | Pending |
| 3C-2 | Edge: signed offline bundles + rollout, prefetch + bandwidth policy manifest blocks, resumable (Range) download; Bundle Manager + Edge dashboard | P3-M06 | Pending |
| 3D-1 | Ads: inventory/bookings/playback links, billing-ready aggregates + ad performance report/export, `ads.manage` permission; Ads screens | P3-M05 | Pending |
| 3D-2 | Analytics platform: daily aggregates + semantic metrics module + reconciliation, scheduled data exports; Exports tab | P3-M11 | Pending |
| 3D-3 | Fleet intelligence: anomaly rules/detector/evidence, recommendations, ack + whitelisted remediation; Fleet Intelligence + AI Ops Rules screens | P3-M07 | Pending |
| 3E-1 | Enterprise SSO (OIDC) + claim mapping + audit; SSO screen | P3-GLO-002 | Pending |
| 3E-2 | White label: branding theme + custom-domain metadata + email identity (real SMTP adapter); regional tenancy metadata + platform admin views | P3-GLO-001/003/004 | Pending |
| 3E-3 | Advanced security: device identities + credential lifecycle sweeps, policy engine + violations, security analytics; Security Center | P3-M10 | Pending |
| 3E-4 | Integration catalogue (connectors/instances) + event-bus UI consolidation | P3-INT-101/102 | Pending |
| 3E-5 | Hardening: all 7 SRS §9 acceptance flows, failure-mode matrix (STEP 34), extended load smoke, security review, full regression sign-off | NFR3, §9, §10 | Pending |

Dependency notes: 3A-1 precedes everything (flags + events); 3A-2 precedes
3B-2 (external context) and feeds widgets; 3B-2 precedes 3B-3; 3C-1 sync
block precedes 3C-2 bundle block only in manifest-versioning order; 3D-1
depends on 1I campaigns + 2I PoP; 3D-2 closes 2I deferrals; 3E-2 email
work also closes the Phase-1 password-reset deferral.
