# Phase 2 — Screen Implementation Matrix

Every screen must ship with loading/empty/error states, validation,
permission gating, pagination and search/filter where applicable.
Status: pending → in progress → done (updated per slice).

| SRS | Screen | Route | Purpose | Key components | API dependencies | Permissions | Status |
|---|---|---|---|---|---|---|---|
| P2-01 | Enterprise Dashboard | /dashboard (extend) | Fleet health, approvals inbox count, incidents, deployment KPI | stat cards, health rollup, incident list | /monitoring/summary (ext), /monitoring/fleet-health, /approvals/inbox, /incidents | monitoring.view | pending |
| P2-02 | Device Fleet | /devices (extend) | Advanced filters, bulk actions, rollout status | filter bar, bulk-select table, action modal | /devices (ext filters), /device-groups/{id}/actions, /devices/bulk | devices.view/manage/control | **done** (group bulk actions via Groups tab; per-row multi-select with select-all + bulk toolbar for group/location/tag; saved views apply q+status filters) |
| P2-03 | Device Detail | /devices modal (extend) | Health, screenshot evidence, events timeline, deployments | timeline, screenshot panel | /devices/{id}, /devices/{id}/events, /devices/{id}/screenshots | devices.view | **done** (screenshot evidence + merged event/incident/recovery timeline in the device modal; per-device deployment history remains in Publishing) |
| P2-04 | Device Group Builder | /devices → Groups tab (extend) | Static/dynamic rules + preview count | rule builder, preview | /device-groups (ext), /device-groups/preview | devices.manage | **done** (static/dynamic radio, all/any match, condition rows incl. tag + location subtree, live preview count + sample, member counts + bulk action modal on group cards) |
| P2-05 | Player Update Center | /releases | Packages, rollout rings, progress, rollback | release cards, ring progress | /player-releases, /rollouts | releases.manage | **done** (new release modal with integrated .zip upload, rollout modal with target group + rings + threshold, live ring progress bars, per-device drilldown with failure reasons, rollback with confirm) |
| P2-06 | Template Library | /design → Templates tab | Versions, tags, approval status, usage | template cards | /templates (ext), /approvals | layouts.view/manage | **done** (Design → Templates tab: status + approved-version cards, submit/archive/use-in-layout, create from scratch or designer "Save as template") |
| P2-07 | Template Designer | /design/:id (reuse designer) | Zones, bindings, data variables, preview | designer + binding panel | /templates/{id}/versions, /widgets | layouts.manage | **partial** (designer widget panel: widget select, schema-driven config form, {{token}} bindings with variable hints; full template-native canvas editing routes through layouts + "Save as template") |
| P2-08 | Widget Library | /design → Widgets tab | Widget catalogue, schema, versions | widget cards, schema form | /widgets | widgets.manage | **done** (Design → Widgets tab: catalogue cards with schema chips, create modal with field builder, archive/restore) |
| P2-09 | Content Approval Inbox | /approvals | Pending/returned/approved/rejected queues | inbox tabs, decision modal w/ comments, action trail | /approvals/inbox, /approvals/{id}/approve\|reject | *.approve | **done** (+ policy section in /settings) |
| P2-10 | Campaign Builder | /campaigns modal (extend) | Variants, target rules, blackouts, preview | variant editor, blackout editor, target preview | /campaigns/{id}/variants, /targets/preview | campaigns.manage | **done** (variants section with inline add form — name/playlist/target/priority — and per-variant delete; targets preview endpoint available; blackout editing via Schedules) |
| P2-11 | Schedule Calendar | /schedules (extend) | Month view, recurrence exceptions, blackouts | month grid, exception editor | /calendar (ext), /schedules (ext) | schedules.view/manage | **done** (week/month toggle, blackout chips, conflict highlighting, schedule form with kind + monthly days + exception dates, "Check conflicts" dry-run with winner display) |
| P2-12 | Deployment Center | /deployments (extend) | Rollout jobs, retries, failure reasons | existing + rollout section | /deployments, /rollouts | deployments.view/manage | pending |
| P2-13 | Fleet Monitoring | /monitoring | Health by org/location/group, thresholds | rollup tree, threshold form | /monitoring/fleet-health, org policy | monitoring.view | **done** (org tiles + location/group rollup tables + editable tenant thresholds; map view deliberately out of scope — list rollups per SRS "map/list") |
| P2-14 | Incident Center | /monitoring → Incidents tab | Open/ack/resolved incidents | incident table, ack/resolve | /incidents | incidents.manage | **done** (Monitoring → Incidents tab: open/acknowledged/resolved/all queues, acknowledge + resolve transitions, severity + resolution display) |
| P2-15 | Proof-of-Play Report | /reports (extend) | Lifecycle statuses, reach, export | PoP table + export button | /reports/proof-of-play, /reports/export | reports.view/export | **done** (Reports → Proof of play tab: dimension + date filters, lifecycle table, CSV/XLSX export gated by reports.export) |
| P2-16 | Campaign Analytics | /reports (extend) | Delivery + playback KPI by dimension | KPI tables | /reports/campaign-performance | reports.view | **done** (Reports → Campaign analytics tab: delivery + playback KPI table + export) |
| P2-17 | Report Builder | /reports → Builder tab | Dimensions/filters/columns, export | builder form | /reports/export | reports.export | **partial** (builder-lite: dimension + filter pickers on the PoP tab feed the same export engine; column selection + scheduled exports deferred, documented) |
| P2-18 | Notification Rules | /notifications → Rules tab | Event/condition/channels/escalation | rule form | /notification-rules | settings.manage | **done** (Notifications → Rules tab: rule builder with event catalogue + severity toggles + 3 channels + escalation, enable/disable/delete, per-rule delivery evidence drilldown) |
| P2-19 | Webhook Integrations | /settings → Integrations tab | Subscriptions, secret rotation, delivery log | webhook form, deliveries table | /webhooks, /webhooks/{id}/deliveries | webhooks.manage | **done** (Settings → Webhook integrations: create with one-time secret banner, rotate, enable/disable, delete, delivery log with retry state + replay) |
| P2-20 | API Key Management | /settings → Integrations tab | Create/revoke, scopes, expiry, usage | key table, create modal (one-time secret) | /api-keys | api_keys.manage | **done** (Settings → API keys: create with scope picker + one-time key banner, expiry, revoke, usage display) |
| P2-21 | Tenant Settings | /settings (extend) | Quotas usage, defaults, retention, thresholds | policy forms, quota bars | /organization (ext) | organization.manage | **done** (Settings: quota usage bars + editable limits, retention policy editor with floors, branding via PATCH; thresholds live on Monitoring, approval policies above) |
| P2-22 | Audit Explorer | /audit (extend) | Evidence links, export | deep links, export button | /audit-logs (ext), /reports/export | audit.view | **done** (evidence deep-links per entity type; CSV/XLSX export gated by reports.export + audit.view; actor/entity/action/date filters since Phase 1) |
