# Phase 2 — Implementation Status

Method unchanged from Phase 1: vertical slices (migration → models →
repositories → services → APIs → tests → frontend), full regression suite
green after every slice, docs updated as part of each slice's definition
of done.

| # | Slice | SRS modules | Status |
|---|---|---|---|
| 2A | Approval & governance engine (policies, requests, actions, maker-checker; campaigns + templates wired; approval inbox UI) | P2-M03 | **Done** (2026-08-29). Migration 0011; polymorphic engine with adapter registry (campaign wired; template adapter lands with 2D versioning); per-tenant policies incl. auto-approval when disabled; maker-checker enforced on inbox *and* legacy endpoints; immutable action trail; requester notifications; 9 new tests, full 162-test regression green; live E2E: policy toggle → submit → self-approval blocked → checker approves with comments → trail visible |
| 2B | Advanced device operations (dynamic groups + preview, bulk actions/edit, screenshots evidence, incidents) | P2-M01 (part), P2-M06 (part) | **Done** (2026-08-29). Migration 0012 (device_groups.group_type/rule_json, screenshots, incidents); rule engine with whitelisted fields (manufacturer/platform/model/status/tag/location) and operators (eq/ne/contains/in/in_subtree), 20-condition cap; dynamic groups resolve at publish time via targeting (snapshot stays frozen); group preview endpoint; bulk group commands (active-only, 1000-device cap, audit) and bulk device update (group/location/tags); screenshot evidence upload via player gateway (PNG/JPEG, 5 MB, per-device rate limit, sha256, signed URLs); incident engine with per-episode dedupe + heartbeat auto-recovery (DEVICE_RECOVERED notification) + acknowledge/resolve transitions and `incidents.manage` permission; 8 new tests incl. SRS acceptance #1 (dynamic group publish) and #5 (offline incident auto-recovery), full 170-test regression green; live E2E: dynamic group "manufacturer eq Samsung AND location in_subtree Kolkata" previewed 1 match → created (badge + member count) → bulk SCREENSHOT queued from group card → simulated player polled command, uploaded PNG, acked → evidence + acknowledged command visible in Device Detail |
| 2C | OTA player updates (releases, rollout rings, stop-on-failure, rollback, Update Center) | P2-M01 (part) | **Done** (2026-08-29). Migration 0013 (player_releases, rollout_batches, rollout_devices); release = versioned reference to an uploaded package asset (application/zip now in upload policy; checksum/size copied from asset version); staged rollout with cumulative rings (default 10→50→100, validated strictly increasing to 100), target = device group (static or dynamic, reusing 2B resolution) or all active devices, deterministic ring assignment; update offered pull-based on the heartbeat response (presigned package URL + sha256) only to the in-progress ring of an active release; player ack endpoint (updating/succeeded/failed, success bumps device.player_version); ring recompute row-locked — completion activates the next ring, failure share above the ring threshold stops the whole rollout (later rings halted, ROLLOUT_STOPPED critical notification, per-device failure reasons as evidence); rollback halts + withdraws the offer (downgrades = new release, documented); `releases.manage` permission (Device Manager + Admin); 8 new tests incl. SRS acceptance #2 (pilot ring failure stops rollout with evidence), full 178-test regression green; live E2E: package uploaded → release 2.5.0 → rollout 10/50/100 @5% via Update Center UI → ring-1 player offered, downloaded package (sha256 verified), succeeded → ring 2 activated → forced failure → rings 2–3 stopped, failure reason + red progress in UI, notification raised, no further offers |
| 2D | Content studio (template versions, widget framework, dynamic bindings, collections) | P2-M02 | **Done** (2026-08-29). Migration 0014 (templates +status/current_version_id, template_versions, widgets, widget_versions, asset_collections + items); templates now governed: draft canvas → submit (canvas/widget/binding validation at submit, never at approve) → 2A approval engine entity type "template" (adapter registered in studio service; approve snapshots an immutable TemplateVersion + sets current_version_id; reject returns to editable; auto-approval policy honored); clone/layout-from-template prefer the approved snapshot; templates creatable from scratch or from a layout ("Save as template" in designer); widget framework: org catalogue with field-list config schema (string/number/boolean/select/url/color, validated server-side, rendered as forms client-side), immutable schema versions, fallback content, `widgets.manage` permission (Content Manager); dynamic bindings: approved data-variable catalogue (date/time/device.*/org.name/weather.*), `{{token}}` syntax validated against whitelist; zone.widget {widget_id, config, bindings} added to canvas schema (Phase-1 canvases unaffected); asset collections CRUD + ordered replace-set items + add-to-playlist reuse; 8 new tests + **new migration-parity test** (alembic chain diffed against model metadata — catches model/migration drift that create_all-based tests cannot); full 187-test regression green on SQLite AND PostgreSQL (conftest now honors TEST_DATABASE_URL); live E2E: widget created in Widget Library → layout zone bound to it with config + {{time}}/{{device.location}} bindings in designer panel → saved as template → submitted (validated) → approved via inbox → v1 approved on template card |
| 2E | Advanced campaigns + enterprise scheduling (variants, blackouts, monthly/exceptions, conflict dry-run, month calendar) | P2-M04, P2-M05 | **Done** (2026-08-29). Migration 0015 (campaign_variants + variant targets; schedules +kind/recurrence_json/exception_dates_json); blackouts = schedules with kind='blackout' (documented reuse) suppressing an otherwise-eligible campaign in resolve_active_campaign and shipped to the player for offline evaluation; recurrence extended in place: monthly days-of-month + exception dates, combinable with weekdays, evaluated in the schedule's timezone (incl. overnight-yesterday semantics); variants = audience-specific creative overrides (location/device/group/tag targets via the shared Phase-1 resolver; highest-priority match wins; base campaign is the default; variants change WHAT a device renders, never WHO receives the deployment); manifest resolves the variant per device and swaps layout/playlist + assets; targets preview endpoint resolves a proposed target set without saving; conflict dry-run endpoint expands a proposed schedule against the whole calendar and reports every overlap with the deterministic winner (equal priority flagged as true conflict) — mirrors the runtime resolution rule; calendar events carry kind (blackouts excluded from conflict detection) + /schedules/calendar alias; UI: week/month calendar with blackout chips + dimmed adjacent-month days, schedule form with kind/monthly/exceptions + "Check conflicts" dry-run panel, variants section in campaign modal; 10 new tests incl. SRS acceptance #4 (overlap → conflict + deterministic winner before publish), full 197-test regression green on SQLite (PostgreSQL run per slice gate); live E2E: blackout created via UI → dark calendar chips + kind badge; dry-run showed 32 overlaps with winner Store Hours Promo (p60>p55); month view rendered; variant added via campaign modal → Kolkata device manifest returned variant + overridden playlist + both schedule kinds |
| 2F | Monitoring & fleet health (rollups, per-tenant thresholds, incident center UI) | P2-M06 | **Done** (2026-08-29). Migration 0016 (organizations.settings_json — tenant policy store; notification tables planned for 0016 shift to a later revision); per-tenant monitoring thresholds (P2-MON-002: warning/offline seconds, storage alert %, min player version; validated, audited, platform defaults overlaid; GET monitoring.view / PUT settings.manage); connection_status + detect_offline_devices honor tenant thresholds; storage enforcement on heartbeat (incident per episode + DEVICE_STORAGE notification above the limit, auto-resolve below); fleet-health rollups (P2-MON-001: organization tiles incl. open incidents + outdated players via semver-aware comparison, location subtree rollups off materialized paths, group rollups incl. dynamic rules); device event timeline endpoint (P2-MON-004: player events + incident opens + recoveries merged, newest first); UI: new Monitoring nav — Fleet health tab (tiles, indented location table, group table, editable thresholds) + Incident Center tab (P2-14: open/acknowledged/resolved queues with transitions), device modal event timeline (completes P2-03); 9 new tests, full 206-test regression green on SQLite; live E2E: threshold set to 80% via UI → player heartbeat at 92% opened the incident (visible in Incident Center) → acknowledged via UI → heartbeat at 48% auto-resolved it ("Storage back at 48%") → timeline in device modal shows incident + recovery dots |
| 2G | Notification rules, escalation, channels (in-app/email/webhook) over event dispatch | P2-M08 | **Done** (2026-08-29). Migration 0017 (notification_rules, notification_deliveries); rules map an event type (from a published catalogue incl. `*` wildcard) + optional severity condition to 1..10 channels; dispatch rides on notifications.create() so every producer (offline sweep, storage checks, approvals, OTA) is covered with zero per-producer wiring, and rule errors are isolated from the business action; in-app delivery = the notification itself, recorded as evidence; email via a pluggable provider (logging provider in dev, mirroring the LocalStorage/S3 adapter pattern — SMTP is a deployment swap); webhooks NEVER fire inline — queued pending and pushed by process_pending_deliveries with attempts + failure capture (max 3); escalation (P2-NTF-002): unread rule-matched notifications past the rule's delay re-fire once as critical ESCALATION (idempotent, engine-produced types excluded from matching so escalations cannot cascade); delivery evidence API + per-rule drilldown; UI: Notifications → Rules tab (create with event catalogue/severity toggles/channel inputs/escalation, enable/disable/delete, per-rule delivery evidence); 8 new tests, full 214-test regression green on SQLite; live E2E: rule created via UI (DEVICE_STORAGE, warning, all 3 channels, escalate 15m) → player heartbeat at 96% → in_app + email delivered instantly, webhook delivered by the sweep to a real local HTTP receiver (payload verified) → backdated unread alert escalated exactly once as critical ESCALATION → all three Delivered rows visible in the rule's evidence drilldown |
| 2H | Integrations (webhooks with signed retries + dead-letter, scoped API keys + auth path) | P2-M10 | **Done** (2026-08-29). Migration 0018 (webhook_subscriptions, webhook_deliveries, api_keys); webhook subscriptions to the operational event stream (same catalogue as notification rules, incl. `*`); every delivery signed with HMAC-SHA256 of the exact body (X-Webhook-Signature + event/delivery/attempt headers); deliveries enqueued inline but pushed ONLY by the DB-backed worker sweep (survives restarts, NFR2-08) with exponential backoff (1m·2m·4m·8m) into a replayable dead-letter state after 5 attempts (P2-INT-003); signing secret revealed once at create/rotate, never in list output or audit rows (NFR2-05 policy documented: secret stored server-side for HMAC, managed secret storage is a deployment concern); scoped API keys: raw `dsk_` key shown once, only SHA-256 stored; scopes = permission-code subset enforced by the same require_permissions guard via a transient X-API-Key principal (tenant from the key, attributed to its creator for audit); expiry + immediate revocation + last_used tracking; new `webhooks.manage` permission (api_keys.manage narrowed to keys); UI: Settings → Webhook integrations (create w/ one-time secret banner, rotate, enable/disable, delivery log w/ replay) + API keys (create w/ scope picker + one-time key banner, revoke, usage); 7 new tests, full 221-test regression green on SQLite; live E2E: webhook + API key created via UI (secrets revealed once) → storage event → worker sweep POSTed the signed delivery to a local receiver that independently verified the HMAC (signature_valid=True, HTTP 200) → Delivered row w/ response code in UI → API key returned 200 in scope, 403 out of scope, 401 for bad key, last_used recorded |
| 2I | Reporting & exports (uptime, PoP lifecycle, campaign analytics, CSV/XLSX, report builder) | P2-M07 | **Done** (2026-08-29). No schema change (playback_events + device_heartbeats from 0010 suffice — deliberate reuse); proof-of-play (P2-RPT-001): lifecycle rollup (plays, completed, completion rate, reach, first/last play) by a chosen dimension — campaign/asset/device/location — with date/campaign/location-subtree filters; this dimension+filter engine IS the report-builder core (P2-17 builder-lite: columns fixed per report, scheduled exports out of scope, documented); campaign performance (P2-RPT-002): delivery KPI (acked/pending/failed from fan-out) merged with playback KPI per campaign; device uptime (P2-RPT-003): heartbeat-window algorithm — each beat covers min(gap-to-next, tenant offline threshold), capped trailing window; maintenance exclusions not modeled (documented deviation); exports (P2-RPT-004): POST /reports/export renders ANY of the 6 catalogue reports as CSV (UTF-8 BOM, Excel-friendly) or XLSX (dependency-free minimal OOXML package, inline strings) with filename + audit trail; PDF deliberately out; new `reports.export` permission (Content Manager + Admin); UI: Reports page re-tabbed — Overview (Phase-1 tables) / Proof of play (dimension + date pickers + export) / Campaign analytics / Uptime (colored % bars); api.download helper saves exports with the server filename; 7 new tests incl. SRS acceptance #6 (PoP by location + campaign, exported, counts reconciled against the raw events), full 228-test regression green on SQLite; live E2E on PG: seeded 6 playback events via two real players → PoP showed plays=6/completed=5/devices=2 (exact reconciliation), performance merged acked=2 with plays, uptime ranked the demo fleet, CSV downloaded with correct filename + BOM header, XLSX validated as a proper zip package; all four tabs rendered |
| 2J | Enterprise search, saved views, bulk edit | P2-M09 | **Done** (2026-08-29). Migration 0019 (saved_views); global search (P2-SRC-001): GET /search?q= sweeps devices (name+serial), content, locations, campaigns, playlists, schedules and users in one query — strictly permission-filtered (a module the caller cannot view is absent entirely, verified with a devices.view-only custom role), ≤10 rows/module, min 2 chars; saved views (P2-SRC-002): personal (owner-scoped even within the org), per-module (whitelist: devices/content/campaigns/playlists/schedules/audit/incidents), filter_json + columns_json, name unique per owner+module (409); bulk edit (P2-SRC-003): backend from 2B, UI completed — multi-select checkboxes (select-all-on-page) + bulk toolbar (assign group, assign location, add tag key=value) over POST /devices/bulk; UI: global search box in the app header with debounced grouped dropdown; Devices page gains saved-views dropdown (apply restores q+status), inline save-view form, delete; 5 new tests, full 233-test regression green on SQLite; live E2E: header search "salt" returned grouped device + location results with status chips; two devices multi-selected and bulk-tagged region=east (API-verified on exactly those two; a payload-shape bug found and fixed during the E2E); saved view "Active fleet" created via UI, persisted, listed after reload and applied (status filter restored) |
| 2K | Tenant administration + audit/compliance (quotas, policy defaults, retention pruning, evidence links, audit export) | P2-M11, P2-M12 | **Done** (2026-08-29). No new migration (quotas_json since 0001-era schema, settings_json since 0016); quotas (P2-TNT-002): max_devices/max_users/max_storage_mb in organizations.quotas_json, GET /organization/usage (live usage + limits), PATCH /organization/quotas (organization.manage, audited); ENFORCED at the creation choke points — device registration (422 at cap; idempotent re-polls unaffected), user creation, upload sessions (projected size vs stored bytes); branding (P2-TNT-001) via the existing PATCH /organization branding_json; defaults (P2-TNT-003) realized across the tenant policy stores (approval policies 2A, monitoring thresholds 2F, retention here); retention (P2-AUD-003/NFR2-06): per-tenant settings_json.retention_days with platform floors/ceilings — audit logs floored at 90 days — GET/PUT /organization/retention (settings.manage, audited), prune_retention maintenance sweep deletes per-org rows past window across heartbeats/events/playback/notifications/webhook_deliveries/audit_logs and leaves a RETENTION_PRUNED audit record; audit export (P2-22): 'audit' joined the export catalogue (CSV/XLSX, action/entity/date filters) requiring audit.view ON TOP of reports.export; evidence links (P2-AUD-002): audit rows deep-link entity types to their module routes; UI: Settings gains Quotas & Usage (bars + editable limits) and Data Retention sections; Audit page gains export buttons + entity links; 7 new tests, full 240-test regression green on SQLite; live E2E: retention set to 14d, audit floor rejected (30<90 → 400), device quota 3/3 blocked the 4th registration with exact message, audit CSV exported filtered by action |
| 2L | Hardening: performance, security review, acceptance scenarios, regression sign-off | NFR2, §8, §9 | **Done** (2026-08-29). All six SRS §8 acceptance scenarios automated AND demonstrated live (mapping table in PHASE_2_TEST_PLAN.md); Phase-2 sweeps wired into the Celery maintenance worker + beat schedule (rule webhook pushes 60s, subscription webhook deliveries 60s, escalations 5m, offline detection 2m, retention pruning daily) — all DB-backed and restart-safe (NFR2-08); load smoke re-run on the full Phase-2 backend: 50-device fan-out 135ms vs Phase-1 128ms baseline (no regression), 50/50 acks published, heartbeat p95 queueing-dominated at concurrency 50 on one worker (script now handles maker-checker policy); **test infrastructure moved to PostgreSQL-only** (user directive): digital_app_test auto-provisioned, TRUNCATE isolation, parity test migrated to a scratch digital_app_parity PG database, aiosqlite dependency removed, test-env Argon2 tuned to minimum cost (~4× faster suite); security posture: ruff S (bandit) gate green, secrets absent from responses/audit (tested), rate limits + security headers from P1-12 intact, per-resource cross-tenant probes in every slice's tests; final regression: 240 tests green on PostgreSQL |

Dependency notes: 2A precedes 2D/2E (approval hooks); 2B precedes 2C
(groups drive rings) and 2F (incidents); 2G precedes 2H's delivery reuse;
frontend for each slice ships within the slice (no mock data).
