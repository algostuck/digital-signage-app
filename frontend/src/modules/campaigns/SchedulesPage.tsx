/** Campaigns › Schedule: the scheduling command center
 * (docs/SCHEDULE_UX_AUDIT.md). The route keeps this module name; the
 * implementation lives in ./schedule. */
export { ScheduleWorkspace as SchedulesPage } from "./schedule/ScheduleWorkspace";
